<div class="container">
<?php
if (isset($successMessage)): ?>
  <div class="alert alert-success alert-dismissible fade in">
    <?= htmlspecialchars($successMessage) ?><a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
  </div>
<script>
  setTimeout(function(){ $(".alert").slideUp() }, 5000);
</script>
<?php
endif; ?>
  
  <h1>Einstellungen</h1>
  <div class="panel panel-default">
    <div class="panel-heading">
      <h4 class="panel-title">Benachrichtigungen</h4>
    </div>
    <div class="panel-body">
      <form method="post" action="<?=K_BASE_URL?>/settings">
        E-Mail senden, wenn
        <div class="checkbox"><label>
          <input type="checkbox" name="notify_neworder" <?=$user->notify_neworder ? "checked" : ""?> value="1">
          eine neue gemeinsame Bestellung angelegt wurde
        </label></div>
        <div class="checkbox"><label>
          <input type="checkbox" name="notify_orderdue" <?=$user->notify_orderdue ? "checked" : ""?> value="1">
          eine eigene Bestellung fällig ist
        </label></div>
        <div class="checkbox"><label>
          <input type="checkbox" name="notify_orderready" <?=$user->notify_orderready ? "checked" : ""?> value="1">
          die gemeinsame Bestellung geliefert wurde
        </label></div>
        <div class="checkbox"><label>
          <input type="checkbox" name="notify_newfile" <?=$user->notify_newfile ? "checked" : ""?> value="1">
          eine Datei hochgeladen wurde
        </label></div>
        <div class="form-group">
          <label class="control-label">PayPal</label>
          <div class="input-group">
            <span class="input-group-addon">paypal.me/</span>
            <input type="text" class="form-control" name="paypal" value="<?=htmlspecialchars($user->paypal)?>" placeholder="username">
          </div>
        </div>
        <button type="submit" class="btn btn-primary">Speichern</button>
      </form>
    </div>
  </div>
</div>
